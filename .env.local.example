# Offline RAG configuration (no external services required)
# Default values assume docker-compose deployment.
CHROMA_URL=http://localhost:8000
OLLAMA_URL=http://localhost:11434

# Local model identifiers served by Ollama
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_GENERATE_MODEL=mistral:7b

# Optional: directory where curriculum/insight JSON exports live
DATA_DIR=./data
